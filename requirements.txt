# =============================================================
#  requirements.txt – GIS Zonal & Temporal Statistics Toolkit
#  Install with: pip install -r requirements.txt
#
#  ⚠️  GDAL/rasterio/fiona note:
#     On Linux/macOS, GDAL system libraries must be installed first:
#       Ubuntu/Debian : sudo apt-get install gdal-bin libgdal-dev
#       macOS (brew)  : brew install gdal
#     On Windows, use pre-built wheels from:
#       https://github.com/cgohlke/geospatial-wheels/releases
#     Alternatively, use the environment.yml with Conda to avoid
#     manual GDAL setup.
# =============================================================

# -- Core geospatial ------------------------------------------
GDAL>=3.6
rasterio>=1.3
rasterstats>=0.19
geopandas>=0.13
pyproj>=3.5
shapely>=2.0
fiona>=1.9

# -- Data science ---------------------------------------------
numpy>=1.24
pandas>=2.0
scipy>=1.11

# -- Visualisation --------------------------------------------
matplotlib>=3.7
seaborn>=0.12
folium>=0.14
mapclassify>=2.5
branca>=0.6

# -- Notebook -------------------------------------------------
jupyter>=1.0
jupyterlab>=4.0
ipywidgets>=8.0
ipython>=8.0
nbformat>=5.7

# -- I/O helpers ----------------------------------------------
openpyxl>=3.1
pyarrow>=12.0

# -- Utilities ------------------------------------------------
tqdm>=4.65
colorama>=0.4

# -- Optional -------------------------------------------------
localtileserver>=0.10   # Local raster tile server for Folium previews
